import { createFileRoute,Link } from "@tanstack/react-router";import { useState } from "react";import { supabase } from "@/integrations/supabase/client";import { Button } from "@/components/ui/button";import { Input } from "@/components/ui/input";import { Label } from "@/components/ui/label";import { Brand } from "@/components/crimeconnect/brand";
export const Route=createFileRoute("/forgot-password")({head:()=>({meta:[{title:"Reset Password — CrimeConnect"},{name:"description",content:"Request a secure CrimeConnect password reset link."},{property:"og:title",content:"Reset CrimeConnect Password"},{property:"og:description",content:"Secure account recovery."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary"}]}),component:Page});function Page(){const[email,setEmail]=useState("");const[msg,setMsg]=useState("");async function submit(e:React.FormEvent){e.preventDefault();await supabase.auth.resetPasswordForEmail(email,{redirectTo:window.location.origin+"/reset-password"});setMsg("If that account exists, a secure reset link is on its way.")}return <main className="flex min-h-screen items-center justify-center p-4"><div className="w-full max-w-md rounded-lg border border-border bg-card p-7"><Brand/><h1 className="mt-8 text-2xl font-bold">Reset your password</h1><p className="mt-2 text-sm text-muted-foreground">Enter your account email to receive a recovery link.</p><form className="mt-6 space-y-4" onSubmit={submit}><div><Label htmlFor="email">Email</Label><Input id="email" type="email" className="mt-2 h-11" value={email} onChange={e=>setEmail(e.target.value)} required/></div>{msg&&<p className="text-sm text-success">{msg}</p>}<Button className="w-full" size="xl">Send reset link</Button></form><Link to="/auth" search={{mode:"login"}} className="mt-6 block text-center text-sm font-bold text-primary">Back to login</Link></div></main>}