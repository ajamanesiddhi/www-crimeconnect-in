import { createFileRoute,Link } from "@tanstack/react-router";import { useState } from "react";import { supabase } from "@/integrations/supabase/client";import { Button } from "@/components/ui/button";import { Input } from "@/components/ui/input";import { Label } from "@/components/ui/label";
export const Route=createFileRoute("/reset-password")({head:()=>({meta:[{title:"Set New Password — CrimeConnect"},{name:"description",content:"Choose a new secure CrimeConnect password."},{property:"og:title",content:"Set New CrimeConnect Password"},{property:"og:description",content:"Complete secure account recovery."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary"}]}),component:Page});function Page(){const[p,setP]=useState("");const[msg,setMsg]=useState("");async function submit(e:React.FormEvent){e.preventDefault();if(p.length<8){setMsg("Use at least 8 characters.");return}const{error}=await supabase.auth.updateUser({password:p});setMsg(error?error.message:"Password updated. You can now continue securely.")}return <main className="flex min-h-screen items-center justify-center p-4"><form onSubmit={submit} className="w-full max-w-md rounded-lg border border-border bg-card p-7"><h1 className="text-2xl font-bold">Set a new password</h1><p className="mt-2 text-sm text-muted-foreground">This page accepts a valid recovery session from your email link.</p><div className="mt-6"><Label htmlFor="password">New password</Label><Input id="password" type="password" value={p} onChange={e=>setP(e.target.value)} className="mt-2 h-11" required/></div>{msg&&<p className="mt-4 text-sm">{msg}</p>}<Button className="mt-5 w-full" size="xl">Update password</Button><Link to="/auth" search={{mode:"login"}} className="mt-5 block text-center text-sm font-bold text-primary">Return to login</Link></form></main>}